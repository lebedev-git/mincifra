# CLAUDE.md

Это платформа подготовки ПО. Сейчас активен ОДИН сценарий — **подача в Роспатент**.

**Твоя инструкция — в корневом [CLAUDE.md проекта](../../CLAUDE.md)** (C:\Disk D\Project\Mincifra\CLAUDE.md):
старт-диалог, выбор продукта, `prepare_rospatent`, выдача двух блоков (поля Госуслуг + файлы).

Минцифра-трек (AGENTS.md) заморожен — не используй его, пока пользователь явно не попросит.
Когда попросит: требования берутся из выписки акта `99_reference/pp1236.json` через
инструмент `check_compliance`, а не по памяти.
